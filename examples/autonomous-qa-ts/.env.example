# Copy to .env locally. Never commit the filled-in file.
SOLARI_API_KEY=
QA_PROVIDER=anthropic
ANTHROPIC_API_KEY=
# For Experiential: set QA_PROVIDER=experiential and use this key instead.
EXPLABS_API_KEY=
# An available model ID from the selected provider account.
QA_MODEL=
# Optional: only used after explicit approval of an exact GitHub issue draft.
GITHUB_TOKEN=
